# Edge Functions

Toda función nueva debe definir autenticación, autorización, CORS, rate limiting cuando aplique, validación de entrada, logging seguro y pruebas. `verify_jwt=false` requiere una justificación explícita y autenticación alternativa demostrada.
